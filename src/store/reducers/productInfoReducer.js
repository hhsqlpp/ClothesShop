const initialState = {
    loading: true,
    error: false,
    productInfo = {}
}

export default function(state = initialState, action) {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}